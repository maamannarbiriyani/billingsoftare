import { LandingClient } from "@/components/landing/LandingClient";

export const metadata = {
  title: "Enterprise Billing OS V2.0",
  description: "The modern operating system for retail businesses. Lightning-fast billing, intelligent inventory, and deep financial analytics.",
};

export default function Page() {
  return <LandingClient />;
}
